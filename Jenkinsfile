pipeline {
	
  agent any
	
  stages {
	 stage('Build') {
               steps {
                      echo 'Build Block'
                     }	
	 }
	stage('Test') {
               steps {
                     echo 'Test Block'
                    }	
	}
	stage('Deploy') {
               steps {
                     echo 'Deploy Block'
                  }	
        }
  }
}
